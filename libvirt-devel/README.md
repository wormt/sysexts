# libvirt-devel

Development headers, pkg-config files and unversioned shared library symlinks
for the libvirt API, to build software against `libvirt` on an image based
system.

## Compatibility

This sysext should be compatible with all Fedora variants (CoreOS, Atomic
Desktops, etc.) but has only been tested on Atomic Desktops.
